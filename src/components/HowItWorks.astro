<section id="how-it-works" class="py-20 sm:py-28 bg-surface">
  <div class="max-w-6xl mx-auto px-4 sm:px-6">
    <div class="text-center mb-16 fade-in-up">
      <h2 class="text-3xl sm:text-4xl font-bold text-dark mb-4">How it works</h2>
      <p class="text-lg text-muted max-w-xl mx-auto">Three steps. First time takes 10 minutes. After that, 30 seconds.</p>
    </div>

    <div class="grid md:grid-cols-3 gap-8 lg:gap-12">
      <!-- Step 1 -->
      <div class="fade-in-up bg-white rounded-2xl p-8 border border-border shadow-sm">
        <div class="w-12 h-12 flex items-center justify-center rounded-full bg-gradient-to-br from-primary to-primary-hover text-white text-lg font-bold mb-6 shadow-md shadow-primary/30">1</div>
        <h3 class="text-xl font-bold text-dark mb-3">Upload any file</h3>
        <p class="text-muted leading-relaxed">
          CSV, Excel, PDF, TSV. NetSuite, QuickBooks, Sage, Exact, SAP, Xero, Dynamics, FreshBooks, Zoho Books -
          any ERP export, any language, any chart of accounts. No reformatting required.
        </p>
      </div>

      <!-- Step 2 -->
      <div class="fade-in-up bg-white rounded-2xl p-8 border border-border shadow-sm">
        <div class="w-12 h-12 flex items-center justify-center rounded-full bg-gradient-to-br from-primary to-primary-hover text-white text-lg font-bold mb-6 shadow-md shadow-primary/30">2</div>
        <h3 class="text-xl font-bold text-dark mb-3">Map once, run forever</h3>
        <p class="text-muted leading-relaxed">
          First upload: map your columns and accounts. The system remembers.
          Month 2 onwards - just drop the file.
        </p>
      </div>

      <!-- Step 3 -->
      <div class="fade-in-up bg-white rounded-2xl p-8 border border-border shadow-sm">
        <div class="w-12 h-12 flex items-center justify-center rounded-full bg-gradient-to-br from-primary to-primary-hover text-white text-lg font-bold mb-6 shadow-md shadow-primary/30">3</div>
        <h3 class="text-xl font-bold text-dark mb-3">Review exceptions, not the whole report</h3>
        <p class="text-muted leading-relaxed">
          Your expertise is in knowing <em>why</em> a number moved, not finding <em>which one</em> did.
          We do the finding. You do the knowing.
        </p>
      </div>
    </div>

    <!-- Tagline -->
    <p class="text-center mt-12 text-lg font-semibold text-dark fade-in-up">
      Setup: 10 minutes. Then you are an analyst, not a reconciler.
    </p>
  </div>
</section>
